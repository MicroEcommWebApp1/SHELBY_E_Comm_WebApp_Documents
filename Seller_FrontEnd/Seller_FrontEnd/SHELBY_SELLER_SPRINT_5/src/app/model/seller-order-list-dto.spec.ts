import { SellerOrderListDto } from './seller-order-list-dto';

describe('SellerOrderListDto', () => {
  it('should create an instance', () => {
    expect(new SellerOrderListDto()).toBeTruthy();
  });
});
